const stages=[['01','Clean','Validate coordinates, timestamps, duplicates and missing values.'],['02','Detect','DBSCAN/H3 grouping turns individual detections into spatial events.'],['03','Correlate','PostGIS distance queries add OSM industrial and land-use evidence.'],['04','Measure','Active days, recurrence and temporal span produce persistence.'],['05','Classify','An explainable feature-rule baseline assigns a tentative event class.'],['06','Prioritize','Transparent weighted factors produce risk and recommended action.']];export default function Page(){return <main className="p-7"><div className="eyebrow">Transparent AI</div><h1 className="text-2xl mt-1">Methodology</h1><p className="muted text-sm mt-2 mb-6">The MVP is rule-based. It is architected for a trained XGBoost or Random Forest replacement once a properly labeled dataset exists; no accuracy claim is made.</p><div className="grid grid-cols-3 gap-4">{stages.map(([n,a,b])=><div className="panel p-5" key={n}><div className="text-[#44d3c5] text-xs">{n}</div><h2 className="text-lg mt-2">{a}</h2><p className="muted text-xs mt-2 leading-relaxed">{b}</p></div>)}</div></main>}
