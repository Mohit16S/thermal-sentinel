import {NextRequest,NextResponse} from 'next/server';import {fetchOsmContext} from '@/lib/osm';export const dynamic='force-dynamic';export async function GET(request:NextRequest){const latitude=Number(request.nextUrl.searchParams.get('lat')),longitude=Number(request.nextUrl.searchParams.get('lon'));if(!Number.isFinite(latitude)||!Number.isFinite(longitude))return NextResponse.json({detail:'lat and lon are required'},{status:400});try{return NextResponse.json({...await fetchOsmContext(latitude,longitude),source:'LIVE OPENSTREETMAP'})}catch(error){return NextResponse.json({detail:error instanceof Error?error.message:'OSM unavailable'},{status:503})}}
