import {NextResponse} from 'next/server';import {findEvent} from '@/lib/pipeline';export async function GET(_:Request,{params}:{params:{id:string}}){const e=await findEvent(params.id);return e?NextResponse.json({classification:e.classification,classificationConfidence:e.classification_confidence,evidence:e.evidence,riskScore:e.risk_score,riskLevel:e.risk_level,riskExplanation:e.risk_explanation,riskBreakdown:e.risk_breakdown,persistenceScore:e.persistence_score,anomalyScore:e.anomaly_score}):NextResponse.json({detail:'Event not found'},{status:404})}
