import {NextRequest,NextResponse} from 'next/server';import {fetchFirmsObservations} from '@/lib/firms';import {parseMode} from '@/lib/mode';export const dynamic='force-dynamic';export async function GET(request:NextRequest){if(parseMode(request.nextUrl.searchParams.get('mode'))==='demo')return NextResponse.json({mode:'demo',message:'Use /api/events for bundled synthetic demonstration events'});try{const observations=await fetchFirmsObservations();return NextResponse.json({mode:'live',count:observations.length,observations})}catch(error){return NextResponse.json({mode:'demo',message:'NASA FIRMS unavailable; use demo mode',detail:error instanceof Error?error.message:'Unknown error'},{status:503})}}
