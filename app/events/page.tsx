import EventList from '@/components/EventList';import {getEvents} from '@/lib/api';export default async function Page(){const e=await getEvents();return <main className="p-7"><div className="eyebrow">Event registry</div><h1 className="text-2xl mt-1 mb-2">Thermal Events</h1><p className="muted text-sm mb-6">Clustered and enriched thermal anomalies. All records below are synthetic demonstration data.</p><EventList events={e}/></main>}
