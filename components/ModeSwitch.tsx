import Link from 'next/link';import {DataMode} from '@/types';
export default function ModeSwitch({mode,path='/',warning}:{mode:DataMode;path?:string;warning?:string}){return <div><div className="inline-flex rounded-md border border-[#294249] p-1 bg-[#071013]"><Link href={`${path}?mode=demo`} className={`px-3 py-1.5 rounded text-[10px] font-bold ${mode==='demo'?'bg-amber-400 text-[#071013]':'muted'}`}>DEMO MODE</Link><Link href={`${path}?mode=live`} className={`px-3 py-1.5 rounded text-[10px] font-bold ${mode==='live'?'bg-[#44d3c5] text-[#071013]':'muted'}`}>LIVE MODE</Link></div>{warning&&<div className="text-[10px] text-amber-300 mt-1 max-w-sm text-right">{warning}</div>}</div>}
