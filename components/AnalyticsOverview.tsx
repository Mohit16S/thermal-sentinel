'use client';import {Event} from '@/types';import {Bar,BarChart,CartesianGrid,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';
export default function AnalyticsOverview({events}:{events:Event[]}){const risks=['CRITICAL','HIGH','MEDIUM','LOW'].map(name=>({name,value:events.filter(e=>e.risk_level===name).length}));const classes=Object.entries(events.reduce<Record<string,number>>((acc,e)=>{const key=e.classification.replace('PERSISTENT INDUSTRIAL THERMAL SOURCE','PERSISTENT SOURCE').replace('WILDFIRE / VEGETATION FIRE','WILDFIRE');acc[key]=(acc[key]??0)+1;return acc},{})).map(([name,value])=>({name,value}));const colors=['#ff4d57','#ff903d','#e9cc51','#4bc17b'];return <div className="grid grid-cols-2 gap-4 mt-4"><section className="panel p-4 h-64"><div className="eyebrow">Risk distribution</div><ResponsiveContainer width="100%" height="88%"><PieChart><Pie data={risks} dataKey="value" innerRadius={48} outerRadius={74} paddingAngle={3}>{risks.map((_,i)=><Cell key={i} fill={colors[i]}/>)}</Pie><Tooltip contentStyle={{background:'#0c181c',border:'1px solid #244047'}}/></PieChart></ResponsiveContainer></section><section className="panel p-4 h-64"><div className="eyebrow">Classification distribution</div><ResponsiveContainer width="100%" height="88%"><BarChart data={classes} layout="vertical" margin={{left:20}}><CartesianGrid stroke="#183036"/><XAxis type="number" tick={{fill:'#769096',fontSize:9}}/><YAxis type="category" dataKey="name" width={120} tick={{fill:'#9eb1b5',fontSize:8}}/><Tooltip contentStyle={{background:'#0c181c',border:'1px solid #244047'}}/><Bar dataKey="value" fill="#44d3c5" radius={[0,3,3,0]}/></BarChart></ResponsiveContainer></section></div>}
