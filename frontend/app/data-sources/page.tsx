const sources=[['NASA FIRMS','Active thermal anomalies: brightness, confidence, time, satellite and instrument. Demo uses FIRMS-like synthetic records; live mode requires an API key.'],['OpenStreetMap','Industrial facilities, land use and geographic context. Proximity is evidence—not confirmation. Overpass results are cached in production.'],['Sentinel-2 / Sentinel-1 / Landsat','Contextual verification imagery when available. The demo never invents imagery and reports unavailability explicitly.'],['Historical FIRMS','Repeated detections in a location support recurrence and persistence analysis. Historical data should be stored as Parquet and PostGIS records.']];export default function Page(){return <main className="p-7"><div className="eyebrow">Provenance & limitations</div><h1 className="text-2xl mt-1 mb-6">Data Sources</h1><div className="grid grid-cols-2 gap-4">{sources.map(([a,b])=><section className="panel p-6" key={a}><h2 className="text-lg">{a}</h2><p className="text-sm muted mt-3 leading-relaxed">{b}</p></section>)}</div><div className="panel border-amber-500/30 p-5 mt-4 text-sm"><b className="text-amber-400">Current mode: SYNTHETIC DEMONSTRATION DATA.</b> No displayed observation, industrial correlation, satellite image, model accuracy, or fire confirmation should be interpreted as real.</div></main>}
